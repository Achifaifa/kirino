TO-DO

Dungeon.py:
	Complete debugger (Make sure the exit is reachable from the entrance)
	add colour to map()
	fix advanced mapping (It does really weird stuff)

Mob.py:
	Add stats

Launch.py:
	Code the main stuff (Test and debug mode)
	Draw function(Takes data from the dungeon, mob and player objects and draws the stuff
	Decide if it would be better to put the drawing function in launch.py or dungeon.py
	Menu (BORIIIING)
	Options, help, loading and saving, etc etc
	
